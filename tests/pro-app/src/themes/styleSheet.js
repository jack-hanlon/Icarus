import { StyleSheet } from "react-native";
import colors from 'src\themes\colors.js'

const styles = StyleSheet.create({
    basicText: {
        color: colors.textPrimary,
        margin: 10,
    },
})